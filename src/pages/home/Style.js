import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  home: {
    width: "100vw",
    height: "auto",
  },

  body: {
    width: "100vw",
    height: "auto",
    display: "flex",
    justifyContent: "center",
  },
}));
